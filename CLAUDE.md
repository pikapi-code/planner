# Git commits

Do not add a `Co-Authored-By: Claude ...` trailer (or any `Claude-Session:` line) to
commit messages or PR descriptions in this repo. The user does not want Claude showing
up as a contributor on GitHub. Commit as the user only, with a plain commit message.
